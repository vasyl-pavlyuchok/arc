# ARC Manifest
# =============
# Controls which domains are active and how they're triggered.
#
# Format:
#   DEVMODE=true|false                (debug output in responses)
#   {DOMAIN}_STATE=active|inactive    (whether domain is enabled)
#   {DOMAIN}_ALWAYS_ON=true|false     (load every prompt, no keyword matching)
#   {DOMAIN}_RECALL=keyword1,keyword2 (triggers on-demand loading)
#   {DOMAIN}_EXCLUDE=word1,word2      (skip domain if these words in prompt)
#   {DOMAIN}_PATH=/abs/path           (auto-load when tool calls touch this path)
#
# Loading Logic:
#   1. ALWAYS_ON=true domains load every prompt (SHORT variants only)
#   2. Other active domains load when recall keywords match user prompt
#   3. Exclusions are checked before matching
#   4. PATH detection auto-loads domain when recent tool calls touch that path

# ============================================================================
# SETTINGS
# ============================================================================
DEVMODE=false

# ============================================================================
# GLOBAL — Universal rules (always loaded, SHORT variants only)
# ============================================================================
GLOBAL_STATE=active
GLOBAL_ALWAYS_ON=true

# ============================================================================
# CONTEXT — Dynamic bracket rules based on context window usage
# ============================================================================
CONTEXT_STATE=active
CONTEXT_ALWAYS_ON=true

# ============================================================================
# COMMANDS — Star-commands like *dev, *review, *brief
# Detected via *pattern matching in user prompt
# ============================================================================
COMMANDS_STATE=active

# ============================================================================
# DOCKER — Infrastructure & containers
# ============================================================================
DOCKER_STATE=active
DOCKER_ALWAYS_ON=false
DOCKER_RECALL=docker, container, compose, traefik, build, deploy, image, port, production

# ============================================================================
# Add your own domains below following the same pattern:
#
# MYPROJECT_STATE=active
# MYPROJECT_ALWAYS_ON=false
# MYPROJECT_RECALL=keyword1, keyword2, keyword3
# MYPROJECT_PATH=/absolute/path/to/project   (optional: auto-load by file path)
# ============================================================================
